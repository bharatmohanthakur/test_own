# Research Findings v2 — March 19, 2026

## CRITICAL: Metric Code vs Eval Page Mismatch
The evaluation page shows different params than the actual metric code defaults:
- Eval page: temp=0.0, max_tokens=7680, max_model_len=8192
- Metric code defaults: temp=1.0, max_tokens=3584, max_model_len=4096
- Ryan Holbrook (Kaggle staff) said: "parameters on the Evaluation page override defaults"
- **CONCLUSION**: Use eval page params (temp=0.0, max_tokens=7680) as truth

## Top 10 Actionable Changes (priority order)

1. **Real CoT traces** (+15-30%): Replace generic filler with actual step-by-step solving
   - Already done in generate_cot_data.py

2. **Add QKV attention to LoRA targets** (+3-8%):
   - Change: `r".*\.(q_proj|k_proj|v_proj|o_proj|in_proj|out_proj|up_proj|down_proj|gate_proj)$"`
   - Model has 6 Attention layers + 23 Mamba+MoE layers, attention handles cross-token reasoning

3. **Increase MAX_SEQ_LEN to 4096** (+3-7%): Matches eval max_model_len

4. **Focus on equation_transform** (+5-10%): Hardest type, solver can't handle it

5. **Train for temp=1.0 robustness** (+2-5%): Data augmentation, consistent \boxed{} format

6. **Try alpha=2*rank (64)** (+2-5%): Stronger adaptation

7. **LR=2e-4** (+1-3%): Unsloth recommended default

8. **75/25 reasoning/non-reasoning mix** (+1-3%): Prevent overthinking simple puzzles

9. **DO NOT fine-tune MoE router** (prevents -20-40% regression)

10. **Match solver output format** for gravity/unit_conversion (exact decimal places)
